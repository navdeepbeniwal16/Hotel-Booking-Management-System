import { render, screen } from '@testing-library/react';
import App from './App';

test('renders learn react link', () => {
  // TODO: learn how to mock API calls @levimk
  // render(<App />);
  // const linkElement = screen.getByText(/Highest number/);
  expect(true).toBe(true);
});
